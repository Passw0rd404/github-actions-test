name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: print hello
        run: echo "hello"
        
      - name: checkout repo
        uses: action/checkout@5.0.0

      - name: list the repo files
        run: ls -la

      
